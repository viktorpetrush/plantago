<% if current_user.admin? %>
  <p class="text-left"><%= link_to "Нова Компанія", new_company_path, class: "btn btn-info btn-sm" %></p>
<% end %>
<div class="table-responsive">
  <table class="table table-stripped table-hover table-condensed">
    <thead>
      <tr>
        <th>ID</th>
        <th>Назва</th>
        <th>Телефон</th>
        <th>Опис</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <%= render @companies %>
    </tbody>
  </table>
</div>
