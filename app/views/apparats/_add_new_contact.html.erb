<%= form_tag add_contact_apparat_path(@apparat), method: :patch do %>
  <%= select_tag :contact_id, options_for_select(@apparat.not_added_contacts.map{|c| [c.name, c.id]}) %>
  <%= submit_tag "Додати контакт" %>
<% end %>
